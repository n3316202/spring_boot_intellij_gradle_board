<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.human.ex.mapper.BoardMapper">

	<select id="getList" resultType="BoardVO">
	<![CDATA[
		select * from mvc_board order by bgroup desc, bstep asc
	]]>
	</select>

	<select id="read" resultType="BoardVO">
	<![CDATA[
		select * from mvc_board where bid = #{bid}
	]]>
	</select>

	<update id="updateShape">
	<![CDATA[
		update mvc_board set bstep = bstep + 1 where bgroup = #{bgroup} and bstep > #{bstep}
	]]>
	</update>

	<insert id="insertReply">
	<![CDATA[
		insert into mvc_board ( bname, btitle, bcontent, bgroup, bstep, bindent) values ( #{bname}, #{btitle},#{bcontent}, #{bgroup}, #{bstep}+1, #{bindent}+1)
	]]>
	</insert>

	<insert id="insertBoard">
	<![CDATA[
		insert into mvc_board ( bname, btitle, bContent, bhit, bgroup, bstep, bindent) values ( #{bname}, #{btitle}, #{bcontent}, 0, (select last_insert_id()+1), 0, 0)
	]]>
	</insert>

	<delete id="delete">
	<![CDATA[
		delete from mvc_board where bid = #{bid}
	]]>
	</delete>

	<update id="updateBoard">
		update mvc_board set bname = #{bname}, btitle =
		#{btitle}, bContent = #{bcontent} where bid = #{bid}
	</update>


	<select id="getTotalCount" resultType="int">
		select count(*) from mvc_board
	</select>

<!--	SELECT * FROM 테이블명 ORDERS LIMIT 숫자(A) OFFSET 숫자(B)-->
<!--	위 쿼리는 (B+1) 행 부터 A 행 만큼 출력을 합니다.-->
	<select id="getListWithPaging" resultType="BoardVO">
  		 <![CDATA[
			select * from mvc_board order by bgroup desc, bstep asc  limit #{amount} OFFSET  #{offset} ;
 		  ]]>
	</select>
</mapper>
